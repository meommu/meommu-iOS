# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'meommu-iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for meommu-iOS

  # 바텀시트 라이브러리
  pod 'PanModal'
  pod 'MaterialComponents/BottomSheet'

  # 네트워크 라이브러리
  pod 'Alamofire' , '5.8'

  # JSON
  pod 'SwiftyJSON'
end
